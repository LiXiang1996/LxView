<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lxview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LxView">
        <activity
            android:name=".home.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".loading.activity.IndicatorActivity"/>
        <activity android:name=".myFreeView.ViewGroup.MyViewGroupActivity"/>
        <activity android:name=".myFreeView.activity.ExOneActivity"/>
        <activity android:name=".myFreeView.activity.SampleActivity"/>
        <activity android:name=".listTimestamp.activity.ListTimeActivity"/>
        <activity android:name=".timePicker.activity.TimePickerActivity"/>
        <activity android:name=".function.home.activity.HomeFunctionActivity"/>
    </application>

</manifest>